spring.application.name=loginjwt

# Veritabanı bağlantısı

spring.datasource.url=jdbc:mysql://localhost:3306/login_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=3527

# JPA ayarları
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Sunucu portu (istersen değiştirebilirsin)
server.port=8080

security.jwt.secret-key=birsirrengizuzunveguclusifrekeykeykeykeykey
